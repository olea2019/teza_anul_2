export enum Argument {
  ASCII = '-a',
  BINARY = '-b',
  ENCRYPT = '-e',
  DELETE = '-d',
  VERIFY = '-v',
  SHORT_NAME = '-n',
  OVERWRITE = '-o',
  NETWORK_FILES = '-z',
  LINK = '-l',
  HELP = '-h',
  INTERACTIVE = '-i',
}
